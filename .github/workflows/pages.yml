name: Publish presentation site

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/pages.yml'
      - 'assets/**'
      - 'bin/**'
      - 'build/icon.svg'
      - 'docs/images/**'
      - 'package.json'
      - 'package-lock.json'
      - 'bun.lock'
      - 'presentations/**'
      - 'README.md'
      - 'src/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11
      - name: Install dependencies
        run: bun ci
      - name: Build presentation library
        run: bun bin/gamma-slides.js library -d presentations -i src/schema/examples/corporate-demo.yaml -o _site
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
